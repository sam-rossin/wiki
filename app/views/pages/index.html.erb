<% if params[:search] %>
  <% provide(:title, 'Search Results') %>
<% else %>
  <% provide(:title, 'Index') %>
<% end %>
<%= render "title" %>

<ul class="pages">
  <%= render @pages %>
</ul>

<%= will_paginate %>